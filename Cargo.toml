[package]
name = "excel-in"
version = "0.1.0"
edition = "2024"

[dependencies]
inkwell = { version = "0.8.0", features = ["llvm21-1"], optional = true }
quick-xml = "0.39.2"
zip = "8.2.0"

[workspace]
members = [".", "crates/viewer", "crates/compiler"]

[features]
jit = ["dep:inkwell"]
